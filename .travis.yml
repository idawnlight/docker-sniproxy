sudo: required

language: bash
services: docker

before_install:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"

script:
  - |
    (
      docker build -t idawnlight/sniproxy:latest .
      docker push idawnlight/sniproxy:latest
    )
after_script:
  - docker images